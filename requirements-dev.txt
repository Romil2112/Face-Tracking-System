# Test/CI dependencies. Pulls the headless API runtime (opencv-python-headless,
# numpy, fastapi, uvicorn, python-multipart) so CI never installs the GUI base.
-r requirements-api.txt
imutils==0.5.4        # main.py imports imutils.video.FPS; needed to import/test it
pytest==9.1.1
pytest-cov==7.1.0
httpx2==2.5.0

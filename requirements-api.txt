# Runtime dependencies for the REST API service (headless OpenCV - no GUI libs).
# opencv pinned <5: opencv-python 5.x fails to initialize the bundled detectors.
opencv-python-headless==4.13.0.92
numpy==2.2.6
fastapi==0.139.0
uvicorn[standard]==0.50.0
python-multipart==0.0.32
